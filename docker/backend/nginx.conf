server {
    listen 80;
    server_name backend;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ =404;
        add_header X-Backend "${CUSTOMER}-${ENVIRONMENT}" always;
    }
}
